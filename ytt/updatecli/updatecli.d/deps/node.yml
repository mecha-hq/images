#! node.yml

name: update node versions

#@ load("fragments.lib.yml", "scm_mecha_images", "actions")
#@ load("fragments.lib.yml", "source_githubrelease")

scms:
  mecha-images: #@ scm_mecha_images()

actions: #@ actions("node")

sources:
  node: #@ source_githubrelease("nodejs", "node", "latest")

targets:
  mise-toml-0:
    name: Bump node version
    kind: toml
    sourceid: node
    scmid: mecha-images
    spec:
      key: .tools.node
      file: mise.toml
